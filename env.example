# ============================================
# CloudFlare R2 설정
# ============================================

# R2 엔드포인트 (빌드 시에만 사용)
VITE_R2_ENDPOINT=https://2d797e9348660f2d5a228739b19cd956.r2.cloudflarestorage.com

# R2 버킷 이름
VITE_R2_BUCKET_NAME=daily-podcast

# ============================================
# 빌드용 R2 API 토큰 (서버 사이드 전용)
# ⚠️ 주의: 이 토큰들은 클라이언트 코드에 포함되지 않습니다.
# 빌드 시에만 사용되며, GitHub Secrets에 저장해야 합니다.
# ============================================
VITE_R2_ACCESS_KEY_ID=your-build-access-key-id
VITE_R2_SECRET_ACCESS_KEY=your-build-secret-access-key

# ============================================
# 런타임용 Public URL (클라이언트에서 사용)
# Public URL을 사용하면 Access Key 없이도 파일에 접근 가능합니다.
# CORS 설정이 필요합니다.
# ============================================
VITE_R2_PUBLIC_URL=https://pub-3a8251d75dec49acaf9d89ec71e37294.r2.dev
